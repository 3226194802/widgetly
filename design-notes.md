# Widgetly 设计笔记与用户偏好

## 毛玻璃效果（Frosted Glass）—— 用户最喜欢的组件背景形态

> 用户原话：「我把它调到 100% 的时候，它不会百分之百透明地显示组件下面的内容，而是既模糊又透明，这种我非常喜欢。」
> 该效果命名为 **毛玻璃效果**，做新组件背景时优先采用。
> **2026-08-15 起「毛玻璃效果」的定义已升级为【实时 backdrop-filter 方案】——以后凡说「毛玻璃效果」即指升级后版本。**

### 效果定义（升级后·实时方案）
组件背景 = **`backdrop-filter: blur(20px) saturate(1.2)` 实时模糊窗口下方真实内容**（移动的窗口、壁纸变化都会实时反映）+ 一层可调透明度的色罩（tint）。**不再使用「截静态壁纸再处理」的旧方案。**

### 技术配方（升级后）
1. 透明窗口 `transparent: true` + `html/body { background: transparent }`。
2. 卡片根元素加 `-webkit-backdrop-filter: blur(20px) saturate(1.2); backdrop-filter: ...`（实时模糊，圆角由 CSS `border-radius` 保证）。
3. 罩层 `.veil`（或 `.bg`）：色罩渐变，`opacity` 由背景透明度滑块控制——**0% = 完全纯色，100% = 全透明（只剩实时模糊）**。
4. 内容层 `z-index` 最高。
- 本机必须 `app.disableHardwareAcceleration()`。backdrop-filter 实时模糊在本机已验证（日历系列、番茄组件均用此方案）。

### 历史（旧方案，已废弃，勿再用）
- **壁纸毛玻璃（旧）**：`desktopCapturer` 截壁纸 → `.glass` 层 `filter: blur()`——静态快照，不实时。dock/monitor 原用此方案，2026-08-15 已升级为实时 backdrop-filter。
- **Mica/亚克力 backgroundMaterial**：会填满整个窗口矩形，真实四角直角；SetWindowRgn 裁圆角在本机 `getSize()` 返回值不稳定（曾把窗口裁成 2/3 + 误伤 Progman 致壁纸全黑），**已放弃，勿再用**。

### 其他已确立的偏好
- 组件背景透明度滑块语义统一为：**0% = 完全不透明（纯色/纯白），100% = 白罩全透明（露出模糊背景）**。
- 滑块用自绘 div（原生 range thumb 在本机拖不动）。
- 本机必须 `app.disableHardwareAcceleration()`，透明大窗口 + 多窗口软件合成会崩溃；管理器用不透明窗口 + 壁纸毛玻璃。

## 组件尺寸规范（用户 2026-08-14 确立，做新组件时按此规则）

> 用户原话：「把我的小组件分成四类：小、中、大、超大。小中这两类里面再分两小类：长条形和正方形……现在只需要记住这些规则，不需要修改任何组件。后面我让你新加的组件，你按照这个规则来就行。」

### 尺寸表（宽 × 高，px）
| 大类 | 子类 | 宽度 | 高度 | 说明 |
|------|------|------|------|------|
| **小号** | 小长条形 | 140 | 74 | = 现「灵动时钟·小」 |
| **小号** | 小正方形 | **65** | **70** | 用户 2026-08-14 更新：宽 65、高 70 |
| **中号** | 中长条形 | 280 | 148 | = 现「灵动时钟·中/标准」「图库·中」「沉浸文件夹」 |
| **中号** | 中正方形 | **140** | **148** | 用户 2026-08-14 更新：宽 140、高 148（非严格正方形，按用户指令） |
| **大号** | 大正方形（唯一） | 285 | 285 | 大号目前只需要这一个正方形 |
| **超大号** | 竖长条（唯一） | 342 | 500 | = 现「AI 用量监控（Hermes）」同款 |

> 尺寸更新记录（2026-08-14，均已按用户指令调整）：
> - **小正方形**：130×140 → **65×70**
> - **中正方形**：270×280 → **140×148**
> - 现有已创建的组件保持原尺寸不变，**以后新建组件按最新规范执行**。

### 使用规则
- 新组件默认按这四档/六形态选择尺寸，不另立尺寸。
- 超大号默认对齐 monitor（342×500）；大号只有一个 285×285 正方形。
- 小号/中号分长条形与正方形两种形态，命名时体现（如「·小长条」「·小方」「·中长条」「·中方」）。
- 窗口创建尺寸 = 上表注册尺寸；拖动引擎锚定/恢复也用该应然尺寸（防漂移）。

## 番茄时钟组件规格（2026-08-15 用户确认方案后建成）

- **形态**：方形悬浮窗，大号 285×285（尺寸规范唯一大正方形），固定不可缩放。
- **背景**：Windows 11 **云母（Mica）半透明**——`backgroundMaterial:'mica'` + `transparent:true` + `hasShadow:false`（与管理器 Acrylic 同套路）；CSS 侧叠半透明罩层（跟随系统明暗）保证文字对比。
- **默认只显示事项列表**，整行悬停时该行右侧浮现圆形 ▶ 计时按钮（移开即消失）；点击 ▶ 进入计时视图（环形倒计时 + 暂停/重置/跳过休息）。
- **番茄循环**：专注(默认25分)→提示音+系统通知→休息(默认5分，每4轮长休息15分)；计时状态机在主进程（时间戳+250ms心跳），重启续跑、休眠唤醒不错时。
- **菜单**：开始专注/暂停/重置/跳过休息 / 添加事项…/删除选中事项 / 设置… / **鼠标穿透** / 置顶显示 / 锁定位置 / 退出。
- **鼠标穿透**：`setIgnoreMouseEvents(true,{forward:true})` 开启后点击穿透到桌面；**悬停到组件上自动恢复可交互，移出后重新穿透**（保证右键菜单永远够得着）。
- 配色：专注=番茄红 #ff6b52→#e8432e，休息=青绿 #41cfc4→#1fa89b；今日完成数徽章在顶栏右侧。
- 数据持久化：tasks / settings / clickThrough / pinned / locked / state / today（今日计数按日期自动重置）。

### 中长条变体（番茄时钟·中长条 280×148，2026-08-15）
- 同一目录（dir: pomodoro）第二入口 `bar.html`，**与方型共用同一适配器**（计时状态机/菜单/设置/穿透全部复用，实例独立）。
- 布局：想象宽度 5 等份——**左 3/5 事项列表**（行=圆圈+名称+🍅数，悬停浮现迷你 ▶），**右 2/5 计时**（状态胶囊+数字+细进度条+任务名+开始/重置/跳过按钮），中间用**番茄色淡影竖线**分隔（1px 渐变线 + 微弱辉光，若隐若现）。
- 计时区常驻显示（非悬停浮现）。

### 云母圆角与透明度（2026-08-15 用户反馈后）
- **真实窗口四角**：`backgroundMaterial:'mica'` 会填满整个窗口矩形，CSS 圆角只作用于内容 → 窗口四角是直角。解法：koffi 调 `dwmapi.DwmSetWindowAttribute(hwnd, 33/*DWMWA_WINDOW_CORNER_PREFERENCE*/, 2/*DWMWCP_ROUND*/, 4)`，让 DWM 把真实窗口角裁圆，与内容圆角一致。
- **背景透明度**：菜单「背景透明度…」弹出组件内自绘滑块面板（与 monitor 同款）；语义 0%=罩层全不透明，100%=罩层全透明只剩云母。持久化 `bgOpacity`（0..1，默认 0.45）。

### 圆角与桌面事故（2026-08-15 用户反馈）
- **圆角基准**：与日历对齐——大号(285×285) 30px、中长条(280×148) 24px；CSS `border-radius` 与 `SetWindowRgn` 区域半径必须一致。
- **⚠️ 重大事故教训**：`SetWindowRgn` 若 hwnd 出错会误伤 **Progman（桌面壁纸层）**——Progman 被裁剪成 187×99 区域 → 整个桌面壁纸变黑、左上角剩一小块壁纸残影。修复：`SetWindowRgn(progman, null, 1)` 清除。
- **护栏**：应用区域前必须 `GetClassNameA` 校验 hwnd 以 `Chrome_WidgetWin` 开头，非 Electron 窗口一律跳过（杜绝再误伤桌面层）。
- 计时按钮的番茄色圆圈**只在计时开始后出现**：未计时时播放按钮保持中性（卡片底色+描边），计时/暂停时才是番茄色渐变圆。大号列表悬停 ▶ 按钮同样中性。
- **⚠️ 多入口共用 CSS 的 ID 污染坑**：中长条(bar.html)与方型(index.html)共用 style.css——style.css 里方型的 `#btnPause`（ID 特异性 1,0,0）会污染中长条的同名按钮（46px+番茄渐变+20px 图标全被套上）。解法：bar.css 里用 `#btnPause.br-btn.play-btn`（1,1,0）显式覆盖。**多入口组件注意检查 style.css 的 ID 选择器。**

### 番茄组件背景与圆角的最终方案（2026-08-15 定稿）
- **背景**：**实时 `backdrop-filter: blur(20px) saturate(1.2)`**（非亚克力——亚克力会填满矩形窗、SetWindowRgn 在本机 `getSize()` 返回值不稳定曾把窗口裁成 2/3 + 误伤 Progman 致壁纸全黑，**已彻底放弃**）；圆角纯 CSS `border-radius`，与日历一致。
- **背景透明度语义（用户 2026-08-15 纠正）**：**0% = 完全纯色，100% = 透明度最高**。即 veil opacity = 1 - 透明度%；曾有个 round-trip 反转 bug（applyOpacity 里 `1-alpha` 应写成 `alpha`）。
- **番茄红色按钮状态逻辑（定稿）**：番茄色圆圈**只在「计时运行中」出现**（`data-running="1"`）；暂停/待机时按钮转中性（卡片底+描边），运行中=双竖暂停图标，暂停=三角形继续图标；暂停时圆环+数字变暗并显示「已暂停」。中长条播放按钮待机点击自动从第一个未完成事项开始。
- **控制按钮三键同尺寸**：方型 46px、中长条 32px；跳过休息图标 = 快进双三角（⏭），非单三角。
- **已删除功能**：鼠标穿透（click-through）——用户 2026-08-15 要求移除。

## 内存监控组件规格（2026-08-15 用户提供参考图后建成）
- **尺寸**：280×180（= 今日待办同尺寸）。
- **背景**：毛玻璃「稍微模糊版」——`backdrop-filter: blur(26px) saturate(1.15)`（比一般毛玻璃更"毛"），边框 `1px rgba(255,255,255,0.10)` 更轻。背景三模式：透明 / 默认毛玻璃 / 纯色。
- **布局**：顶部「内存」+ 右侧若隐若现 1px 细横线；中间 RAM 大百分比（左上，曲线色、Montserrat 800）+ 分数「X.XG / X.XG」（右上）+ 心电图曲线（canvas，36 采样滚动窗口，纵向自动缩放、首帧填满、末端圆点）；底部「存储」+ 至多两个盘符行（盘符 | 百分比 | 进度条 | 「XG / XG」分数）。
- **数据**：RAM = `os.totalmem/freemem`（1s 轮询）；磁盘 = `fs.statfsSync('X:\\')`（30s 轮询，C 盘优先 + 至多 2 盘）。无需 spawn。
- **外观调节**：曲线颜色 / 进度条颜色 / 字体颜色 / 背景模式（透明/毛玻璃/纯色）+ 背景色。
- **free 商用字体**：Montserrat（OFL，数字）+ Nunito（OFL）。

## Agent 平台切换功能（AI 用量监控组件，2026-08-15）
- 标题栏右上角新增 🌐 **平台切换按钮** → 打开平台选择窗口，列出主流 Agent 平台并显示安装检测状态（当前/已安装/未找到）。
- **平台注册表** `widgets/monitor/agents.js`：每个平台 = `detect()`（本地路径检测）+ `fetch()`（读取本地用量数据，返回与 fetch_usage.py 同形状的监控数据）。
- 已支持：**Hermes（桌面版）**（`<hermes 目录>/state.db`，完整）、**DeepSeek Harness（网页版）**（~/.dsh/sessions/**/session.jsonl.zstd，完整）、**Claude Code（命令行）**（~/.claude/projects/**/history.jsonl，完整）、**Codex**（~/.codex/sessions + archived_sessions 的 token_count 事件，完整）、**Gemini CLI / 反重力 Antigravity**（通用 jsonl 扫描，尽力而为）、**Cursor / Trae / 华为云码道 / Copilot / Devin**（仅检测安装）。
- **DeepSeek Harness 读取（用户 2026-08-15 修正数据错误）**：会话文件是**逐帧 zstd 压缩**（`session.jsonl.zstd`，每帧一个 JSONL 记录）。用 Node `zlib.zstdDecompressSync` 按 DSH 官方帧定位算法逐帧解码；`assistant/message`/`assistant/chunk`(usage) 事件含 `data.usage`（inputTokens/outputTokens/cacheReadTokens/reasoningTokens），**时间戳用事件自身 time**（今天用量准确），**模型来自 request/header.config.model**（V4 flash / V4 PRO 拆分）；每 (turn,step) 取最后一次 usage 防重复计数。**文件缓存**（mtime+size）使空闲轮询 10ms；Node 无 zstd 时回退 projcache。文件型平台轮询 15s（hermes 8s）。
- **检测防误报（用户 2026-08-15 反馈）**：必须验证**真实可执行文件**（PATH/npm 全局里的 claude/codex/gemini 命令、Cursor.exe/Trae.exe 等），残留配置目录（~/.gemini、~/.antigravity 等）不算已安装。
- 选择未安装的平台 → 组件显示「未找到此软件「X」」；已安装但无可读数据 → 「已安装「X」，但暂无使用记录」；切换后旧数据清空。
- 平台选择持久化到 config（`platform` 字段），默认 hermes；订阅制平台费用显示「订阅制」，未知计费显示「—」。
- **Token 总量口径（用户 2026-08-15 修正）**：总 Token / 今日 / 本月 / 趋势 / 模型卡 一律 = `input + cache_read + output + reasoning`。**cache_read（缓存读取 token）必须计入**——DSH/Hermes 每次对话携带大量上下文、绝大多数命中 DeepSeek 上下文缓存，cache_read 是绝对大头（今日可达 2 亿，占总量的 ~99%）。曾因排除 cache_read 导致"今日用量显示 223 万 vs 实际 1.8 亿"的错误。

